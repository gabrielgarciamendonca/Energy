import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { TStack } from '../../../routes/Stack/types';

export type SplashProps = NativeStackScreenProps<TStack, 'Splash'>;